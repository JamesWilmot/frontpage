import { safeHtml } from 'frontpage/helpers/safe-html';
import { module, test } from 'qunit';

module('Unit | Helper | safe html');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = safeHtml([42]);
  assert.ok(result);
});
